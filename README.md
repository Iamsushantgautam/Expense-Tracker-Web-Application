# Expense Tracker

This is a full-stack Expense Tracker (PHP + MySQL + HTML/CSS/JS).
Import `db.sql` into MySQL, update `config.php`, ensure `uploads/profile_pics` is writable,
then run with Apache or: `php -S localhost:8000` from the project folder.
